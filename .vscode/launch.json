{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Expo Web",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8081",
            "webRoot": "${workspaceFolder}"
        }
    ]
}